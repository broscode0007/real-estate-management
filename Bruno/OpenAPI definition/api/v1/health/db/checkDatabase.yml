info:
  name: checkDatabase
  type: http
  seq: 1
  tags:
    - database-health-controller

http:
  method: GET
  url: "{{baseUrl}}/api/v1/health/db"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/v1/health/db"
      method: GET
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: "*/*"
      body:
        type: text
        data: "{}"

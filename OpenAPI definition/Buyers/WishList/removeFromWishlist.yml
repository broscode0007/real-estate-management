info:
  name: removeFromWishlist
  type: http
  seq: 4
  tags:
    - wishlist-controller

http:
  method: DELETE
  url: "{{baseUrl}}/api/v1/buyers/wishlist/{{buyer_property_id}}"
  auth:
    type: bearer
    token: "{{buyer_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/v1/buyers/wishlist/:propertyId"
      method: DELETE
      params:
        - name: propertyId
          value: ""
          type: path
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""

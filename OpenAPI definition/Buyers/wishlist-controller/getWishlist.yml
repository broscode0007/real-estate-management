info:
  name: getWishlist
  type: http
  seq: 3
  tags:
    - wishlist-controller

http:
  method: GET
  url: "{{baseUrl}}/api/v1/buyers/wishlist"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/v1/buyers/wishlist"
      method: GET
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: "*/*"
      body:
        type: text
        data: |-
          [
            {
              "id": "",
              "propertyId": "",
              "createdAt": ""
            }
          ]

info:
  name: addToWishlist
  type: http
  seq: 1
  tags:
    - wishlist-controller

http:
  method: POST
  url: "{{baseUrl}}/api/v1/buyers/wishlist/:propertyId"
  params:
    - name: propertyId
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/v1/buyers/wishlist/:propertyId"
      method: POST
      params:
        - name: propertyId
          value: ""
          type: path
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: "*/*"
      body:
        type: text
        data: |-
          {
            "id": "",
            "propertyId": "",
            "createdAt": ""
          }

info:
  name: deleteProfile
  type: http
  seq: 1
  tags:
    - buyer-profile-controller

http:
  method: DELETE
  url: "{{baseUrl}}/api/v1/buyers/profile"
  auth:
    type: bearer
    token: "{{buyer_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/v1/buyers/profile"
      method: DELETE
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""

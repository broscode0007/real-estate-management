info:
  name: test email
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl}}/api/v1/test/email?to=koushikbabuforwork@gmail.com"
  params:
    - name: to
      value: koushikbabuforwork@gmail.com
      type: query
  auth:
    type: bearer
    token: "{{agent_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: deleteProfile
  type: http
  seq: 4
  tags:
    - agent-profile-controller

http:
  method: DELETE
  url: "{{baseUrl}}/api/v1/agents/profile"
  headers:
    - name: Authorization
      value: ""
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/v1/agents/profile"
      method: DELETE
      headers:
        - name: Authorization
          value: ""
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""

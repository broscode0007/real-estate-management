info:
  name: syncAgentWithFirebase
  type: http
  seq: 3
  tags:
    - agent-auth-controller

http:
  method: POST
  url: "{{baseUrl}}/api/v1/agents/auth/sync"
  headers:
    - name: Authorization
      value: ""
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/v1/agents/auth/sync"
      method: POST
      headers:
        - name: Authorization
          value: ""
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: "*/*"
      body:
        type: text
        data: "{}"

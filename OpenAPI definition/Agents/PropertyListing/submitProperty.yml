info:
  name: submitProperty
  type: http
  seq: 6
  tags:
    - property-controller

http:
  method: PATCH
  url: "{{baseUrl}}/api/v1/agents/properties/{{agent_property_id}}/submit"
  headers:
    - name: Authorization
      value: ""
  auth:
    type: bearer
    token: "{{agent_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/v1/agents/properties/:propertyId/submit"
      method: PATCH
      headers:
        - name: Authorization
          value: ""
      params:
        - name: propertyId
          value: ""
          type: path
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: "*/*"
      body:
        type: text
        data: ""
